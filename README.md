# Telemetria de Foguete 🚀

Projeto que verifica se um foguete pode decolar.

## Como executar

Instale as dependências:

```bash
python -m pip install notebook
```

Execute o projeto:

```bash
jupyter notebook
```